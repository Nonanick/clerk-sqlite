import { IEntityProcedure } from 'clerk';

export const Synchronize: IEntityProcedure = {
  name: 'synchronize',
  execute: async (archive, request) => {

    throw new Error('Synchronize not implemented yet!');
  }
};